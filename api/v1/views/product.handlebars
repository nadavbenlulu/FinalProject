<style>
    /* עיצוב דף מוצר בודד */
    .product-details-container {
        direction: rtl;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        padding: 40px 20px;
        max-width: 900px;
        margin: 0 auto;
    }

    .product-main-card {
        display: flex;
        background: white;
        border-radius: 20px;
        box-shadow: 0 4px 20px rgba(0,0,0,0.1);
        overflow: hidden;
        border: 1px solid #eee;
    }

    /* צד ימין - תמונה */
    .product-image-side {
        flex: 1;
        background: #f9f9f9;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 30px;
        border-left: 1px solid #eee;
    }

    .main-img {
        max-width: 100%;
        height: auto;
        object-fit: contain;
        border-radius: 10px;
    }

    /* צד שמאל - מידע */
    .product-info-side {
        flex: 1.2;
        padding: 40px;
        display: flex;
        flex-direction: column;
    }

    .product-category {
        color: #888;
        font-size: 0.9rem;
        margin-bottom: 10px;
        text-transform: uppercase;
    }

    .product-name {
        font-size: 2.5rem;
        font-weight: bold;
        color: #333;
        margin: 0 0 15px 0;
    }

    .product-price-tag {
        font-size: 2.2rem;
        color: #d93025;
        font-weight: bold;
        margin-bottom: 20px;
    }

    .product-desc-text {
        color: #666;
        line-height: 1.6;
        margin-bottom: 30px;
        font-size: 1.1rem;
    }

    /* כפתור קנייה */
    .action-btn {
        background-color: #ffc107;
        color: #000;
        border: none;
        padding: 18px;
        border-radius: 12px;
        font-weight: bold;
        font-size: 1.3rem;
        cursor: pointer;
        transition: all 0.2s;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 10px;
    }

    .action-btn:hover {
        background-color: #eab000;
        transform: scale(1.02);
    }

    .back-link {
        display: inline-block;
        margin-top: 20px;
        color: #007bff;
        text-decoration: none;
        font-weight: 500;
    }
</style>

<div class="product-details-container">
    <div class="product-main-card">
        
        <div class="product-image-side">
            {{#if prod.picname}}
                <img src="/uploads/pics/{{prod.picname}}" class="main-img" alt="{{prod.pname}}">
            {{else}}
                <div style="font-size: 5rem; color: #ddd;">🔌</div>
            {{/if}}
        </div>

        <div class="product-info-side">
            <div class="product-category">מוצרי חשמל | מזהה מוצר: {{pid}}</div>
            <h1 class="product-name">{{prod.pname}}</h1>
            
            <div class="product-price-tag">₪{{prod.price}}</div>

            <div class="product-desc-text">
                זהו מוצר חשמלי איכותי מהקטלוג החדש שלנו. 
                המוצר כולל אחריות מלאה, משלוח מהיר עד הבית ושירות לקוחות 24/7.
                <br><br>
                • עיצוב מודרני<br>
                • צריכת חשמל חסכונית<br>
                • עמידות לאורך זמן
            </div>

            <button class="action-btn" onclick="addToCart('{{prod._id}}')">
                🛒 הוספה לסל וקנייה עכשיו
            </button>

            <a href="/" class="back-link">→ חזרה לכל המוצרים</a>
        </div>

    </div>
</div>

<script>
function addToCart(productId) {
    console.log("שומר מוצר מדף פרטים:", productId);

    fetch('/add-to-cart', {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
        },
        body: JSON.stringify({ productId: productId })
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            alert('המוצר נוסף לסל בהצלחה!');
        } else {
            alert('שגיאה בהוספת המוצר.');
        }
    })
    .catch(error => {
        console.error('Error:', error);
    });
}
</script>






{{!-- <div class="container">
    <div class="row">
        <div class="col">
        "דף מוצר נחמד"  {{pid}} 
        <br> שם המוצר הוא {{prod.pname}}
        <br>המחיר הוא {{prod.price}}  
        </div>
    </div>
</div> --}}